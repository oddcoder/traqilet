# To be used with `cargo about generate about.hbs | fold -s -w 100 | sed 's/[[:space:]]*$//' >THIRD-PARTY.txt`


accepted = [
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "MIT",
    "Unicode-3.0",
    "Zlib",
]

targets = ["x86_64-unknown-linux-gnu", "aarch64-unknown-linux-gnu"]
