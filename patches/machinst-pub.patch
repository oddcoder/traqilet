Upstream: https://github.com/bytecodealliance/wasmtime
Revision: d8a0da6d661605713798c1c9c76be5c28e3159ff (2026-08-28)
Paths:    cranelift/codegen/src/{isa/mod.rs,lib.rs}
License:  Apache-2.0 WITH LLVM-exception

The files below are cranelift's; this patch changes them (Apache-2.0 section 4(b)).

`machinst` and `IsaFlagsHashKey`'s field become `pub`, so a backend can live outside the
crate; the lint line absorbs the glob shadowing and undocumented items this exposes.

diff --git a/src/isa/mod.rs b/src/isa/mod.rs
index 8728c29..6dd6910 100644
--- a/src/isa/mod.rs
+++ b/src/isa/mod.rs
@@ -420,7 +420,7 @@ pub trait TargetIsa: fmt::Display + Send + Sync {
 
 /// A wrapper around the ISA-dependent flags types which only implements `Hash`.
 #[derive(Hash)]
-pub struct IsaFlagsHashKey<'a>(&'a [u8]);
+pub struct IsaFlagsHashKey<'a>(pub &'a [u8]);
 
 /// Function alignment specifications as required by an ISA, returned by
 /// [`TargetIsa::function_alignment`].
diff --git a/src/lib.rs b/src/lib.rs
index 64d37df..77ed7a9 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,5 +1,5 @@
 //! Cranelift code generation library.
-#![deny(missing_docs)]
+#![allow(hidden_glob_reexports, missing_docs)]
 // Display feature requirements in the documentation when building on docs.rs
 #![cfg_attr(docsrs, feature(doc_cfg))]
 #![no_std]
@@ -41,7 +41,7 @@ pub use gimli;
 include!(concat!(env!("ISLE_DIR"), "/isle_numerics.rs"));
 
 #[macro_use]
-mod machinst;
+pub mod machinst;
 
 pub mod binemit;
 pub mod cfg_printer;
