# Its own workspace on purpose: the workspace beside it cannot be resolved until this
# has run, because `[patch.crates-io]` there names a directory this creates.
[workspace]

[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false
